__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Secrets — never commit
*.env
.env
*.env.*
*.key
*.pem
secrets/
# ...but the committed placeholder template IS tracked (no real secrets in it)
!.env.example
# Backups / scratch
*.bak*
*.tmp
*.swp
